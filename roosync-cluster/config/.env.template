# Hermes Agent — Non-secret .env config for po-2026
# This file is copied to /opt/data/.env after each rebuild.
# Secrets (tokens, API keys) are passed via Docker -e flags, NOT here.

# Telegram allowlists
TELEGRAM_ALLOWED_USERS=6541428999
TELEGRAM_GROUP_ALLOWED_USERS=6541428999
TELEGRAM_HOME_CHANNEL=-1003904676273
GATEWAY_ALLOW_ALL_USERS=false
