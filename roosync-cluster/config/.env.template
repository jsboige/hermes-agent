# Hermes Agent — Deployment .env template for po-2026 (z.ai)
# Actual secrets come from Docker env vars (hermes-start.ps1).
# This file only sets non-secret config that Docker env vars don't cover.

# z.ai coding endpoint (NOT the standard paas endpoint)
GLM_BASE_URL=https://api.z.ai/api/coding/paas/v4
